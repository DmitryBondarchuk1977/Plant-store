-- ============================================================
--  009: цена закупки (себестоимость) товара
--  Нужна для маржи в аналитике. Хранится в RUB, как и price.
--  Безопасно к повторному запуску.
-- ============================================================

alter table public.products
  add column if not exists cost_price numeric(12,2);   -- цена закупки, ₽
